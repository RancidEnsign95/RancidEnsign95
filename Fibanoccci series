#include"stdio.h"
int main()
{
    int a=0,b=1,c,limit;printf("Enter the limit ");scanf("%d",&limit);
    for(c=0;c<=limit;)
    {
       c=a+b;
       a=b;
       b=c;
       printf("\n%d",c);
    }
    return 0;
}
